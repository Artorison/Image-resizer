server {
    listen 80;
    server_name localhost;

    location /images/ {
        root /usr/share/nginx/html;
        autoindex on;
    }
    location /images/error.jpg {
        return 500;
    }
}